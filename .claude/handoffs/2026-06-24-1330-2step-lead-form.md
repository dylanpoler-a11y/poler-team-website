# Handoff — 2-step lead-capture form

**Goal:** Replace the single-screen lead popup on `/listing` with a 2-step form to lift the ~3.2% landing-page conversion (the real CPL bottleneck for cold LATAM mobile ad traffic).

**State (DONE + DEPLOYED + VERIFIED):**
- 2-step form live in prod. Step 1 = Full Name (`#lead-name`) + email (`#lead-fields-1`, button "Continue"/"Continuar"). Step 2 = phone + timeline (`#lead-fields-2`, button "Submit & Continue").
- `listing.js`: rewrote the `#lead-form` submit handler (~line 357+) into a two-step flow with `leadStep` state + `goToContactStep()`. Conversion pixel + `/api/save-lead` fire ONCE on Step 2 (`completeLead`, unchanged). Single name split into first/last. `showLeadModal` now focuses `#lead-name` (was `#lead-first`).
- `listing.html`: split modal into `#lead-fields-1` / `#lead-fields-2`; added `#lead-subtitle` id + `#lead-step-indicator`; consent hidden until Step 2; timeline label wired to `data-i18n="timelineLabel"`.
- `i18n.js`: added fullName, continueBtn, contactTitle, contactSubtitle, step1of2, step2of2, timelineLabel, **errSelectTimeline** (EN/ES/PT).
- Cold code-review (code-reviewer agent) → fixed 2 findings (blank timeline label in ES/PT; defensive Step-2 re-validation).
- Local preview verified: Step1→Step2 transition, ES copy, validation gates, name-split, Step-1 fires ZERO pixels.
- **Live save verified end-to-end** then deleted: Airtable rec `recY4OaEPYkuMPS9j` saved Name/First/Last/Phone(+57…)/Timeline/Country=Colombia correctly, then DELETE'd. One test "New Lead" notification went to the team (ZZTEST DeleteMe) — expected, ignore.
- Deployed: `npx vercel --prod --yes` → prod `poler-team-website-ooejhksp2`. `/listing` HTTP 200 serving new markup.

**Pending / NOT done:**
- **Bail-out email capture** (save Step-1 email of people who quit before Step 2) is NOT built. Needs a quiet dedupe endpoint — current `api/save-lead.js` always CREATEs a new record + sends team notification + welcome email, so a naive partial-save would dup leads + spam the team. Kevin wants this as a fast-follow if he asks.
- Pre-existing (NOT mine): duplicate `function initViewToggle()` at listing.js:1724 & :2171 (harmless in browser sloppy mode; `node --check` flags it).

**Touched:** listing.js, listing.html, i18n.js, CLAUDE.md (this file's Lead Capture section), `.claude/launch.json` (added a "static" dev config — inert). Vercel prod deploy. Airtable: 1 test lead created + deleted (net zero).

**Gotchas:**
- Working tree has 36 uncommitted files (vv-ab, ai-calls, flash-config, etc. from prior sessions) — this is the established live state; **the working tree IS prod** (git is behind). Do NOT `git checkout`/reset blindly. Kevin has not OK'd a commit/push to the Dylan-shared repo.
- `OTP_BASE` (listing.js:27) = `https://poler-team-website-two.vercel.app` — the form posts save-lead THERE regardless of where it's served, so even a LOCAL submit creates a REAL Airtable lead.
- Deploy = `npx vercel --prod --yes` (NO git auto-deploy).
